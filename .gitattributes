# Normalize line endings: store everything as LF in the repo.
* text=auto eol=lf

# Binary-ish files that must never be line-ending converted.
*.ged text eol=lf
*.png binary
*.jpg binary
*.ico binary

# Third-party vendored bundles: don't normalize, exclude from language stats.
frontend/vendor/*.js -text linguist-vendored

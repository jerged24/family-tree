# --- runtime ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3
networkx>=3.3
python-multipart>=0.0.9        # file uploads for GEDCOM import

# --- optional GEDCOM helper (cross-check xrefs; hand-rolled parser is primary) ---
ged4py>=0.5.2

# --- dev / test ---
pytest>=8.2
httpx>=0.27                    # FastAPI TestClient transport
pytest-playwright>=0.5         # browser UI (e2e) tests — run `playwright install chromium`
ruff>=0.5
black>=24.4
